import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row'
  },
  textInput: {
    flex: 1,
    borderWidth: 1,
    marginLeft: 5,
  }
})

export default styles
