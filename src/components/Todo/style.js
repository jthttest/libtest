import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  todo: {
    backgroundColor: '#165099',
    color: 'white',
    fontSize: 16,
    fontWeight: '500',
  }
});

export default style;
